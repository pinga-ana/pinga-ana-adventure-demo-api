[project]
name = "pinga-ana-adventure-demo-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.115.6",
    "pymongo==4.10.1",
    "dnspython>=2.6.0",
]

[tool.vercel]
entrypoint = "app.main:app"
